javascript:(function(){ var d = document; var s = d.createElement('script'); s.src = 'http://ajax.googleapis.com/ajax/libs/jquery/1.10.1/jquery.min.js'; d.body.appendChild(s); $('.tweet-text').filter(function(text){ return /#これとか/.test($(this).text()); }).find('a').filter(function(text){ return /http:\/\/instagram.com/.test($(this).text()); }).each(function(){ var url = $(this).attr("href"); window.open(url,'_blank','width=500, height=600'); }); })(); 
